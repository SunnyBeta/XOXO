#include <game.h>

int score(Board *board, const int turn) {
	(void) board;
	(void) turn;
	/* if x is winner  */
	return 100;
}

int minimax(Board *board) {
	(void) board;
	/* Returnt the best move */
	return 1;
}
